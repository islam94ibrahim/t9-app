# T9 application
A ReactJS + Express application that simulates the T9 predictive text old phones keypad

## Requirements
- node

## Installation
- Run `node install` from main directory
- Run `node run start` to run the project locally

## Testing
- Run `npm run test` inside main directory to test backend
- Run `cd client && npm run test` to test frontend